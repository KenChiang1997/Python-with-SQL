CREATE DATABASE `sql_tutorial`; # 反引號 --> 自己命名的名稱的 Data Base. 避免命名的名稱 和 資料庫 Function 的衝突 In the case of, if you want to CREATE DATABASE `database`.
SHOW DATABASES; # Blue text is Function(關鍵字)

CREATE DATABASE `database`;
SHOW DATABASES; # Blue text is Function(關鍵字)

DROP DATABASE `mysql`;
SHOW DATABASES; # Blue text is Function(關鍵字)
